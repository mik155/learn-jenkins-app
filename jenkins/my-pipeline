pipeline {
    agent any
    environment {
        MY_SECRET = 'IS MY DESIRE'
    }

    stages {
        stage('hello-from-gh-repo') {
            agent {
                docker {
                    image 'node:latest'
                    reuseNode true
                }
            }
            steps {
                sh '''
                    ls -la;
                    npm --version;
                    ls -la;
                '''
            }
        }

        stage('test') {
            agent {
                docker {
                    image 'node:latest'
                 }
            }
            steps {
                sh '''
                    npm install;
                    npm build
                    ls -la;
                    ls -la;
                '''
            }
        }
    }
  
}
