pipeline {
    agent any

    stages {
        stage('Check AWS CLI') {
            agent {
                docker {
                    image 'amazon/aws-cli'
                }
            }
            steps {
                sh 'aws --version'
            }
        }
    }
}
