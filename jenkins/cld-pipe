pipeline {
    agent any

    stages {
        stage('Check AWS CLI') {
            agent {
                docker {
                    image 'amazon/aws-cli'
                    args '--entrypoint=\'\''
                }
            }
            steps {
                sh 'aws --version'
            }
        }
    }
}
